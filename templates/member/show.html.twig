{% extends 'base.html.twig' %}

{% block title %}Member{% endblock %}

{% block body %}
    <h1>Member</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ member.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ member.name }}</td>
            </tr>
            <tr>
                <th>Concerts</th>
                <td>{{ member.concerts.count() }}</td>
            </tr>
            <tr>
                <th>Groupe</th>
                <td>{{ member.band.name|default("Aucun") }}</td>
            </tr>
        </tbody>
    </table>

   

    <div class="d-flex">
     <a href="{{ path('member_index') }}" class="btn btn-outline-warning mr-5">< Retourner à la liste</a>
    <a href="{{ path('member_edit', {'id': member.id}) }}" class="btn btn-info mr-5">Modifier</a>

    {{ include('member/_delete_form.html.twig') }}
    </div>
{% endblock %}
